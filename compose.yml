services:
  mc:
    image: itzg/minecraft-server
    tty: true
    stdin_open: true
    environment:
      EULA: "true"
      TYPE: PAPER
      MEMORY: 4G
    ports:
      - "25565:25565"
    volumes:
      - ./data:/data
    restart: unless-stopped
