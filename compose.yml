services:
  mc:
    image: itzg/minecraft-server
    tty: true
    stdin_open: true
    environment:
      EULA: "true"
      TYPE: PAPER
      VERSION: 26.2
      MEMORY: 4G
    ports:
      - "25565:25565"
    volumes:
      - ./data:/data
    restart: unless-stopped
